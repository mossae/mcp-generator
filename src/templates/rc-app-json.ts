export const rcAppJsonTemplate = `{
  "id": "{{manifest.id}}",
  "version": "{{manifest.version}}",
  "requiredApiVersion": "{{manifest.requiredApiVersion}}",
  "name": "{{manifest.name}}",
  "nameSlug": "{{manifest.nameSlug}}",
  "description": "{{manifest.description}}",
  "classFile": "{{manifest.classFile}}"
}
`;
