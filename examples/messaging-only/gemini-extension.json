{
  "name": "rc-messaging-mcp",
  "version": "1.0.0",
  "description": "Minimal Rocket.Chat MCP server (1 tools, ~40 tokens)",
  "mcpServers": {
    "rocketchat": {
      "command": "node",
      "args": ["${extensionPath}/server/dist/index.js"],
      "env": {
        "RC_URL": "${RC_URL}",
        "RC_AUTH_TOKEN": "${RC_AUTH_TOKEN}",
        "RC_USER_ID": "${RC_USER_ID}"
      }
    }
  }
}
